package com.sktech.application.repositories;

import com.sktech.application.entity.ADMISSION_STUDENTS;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ADMISSION_STUDENTS_REPO extends JpaRepository<ADMISSION_STUDENTS, Integer> {
}
